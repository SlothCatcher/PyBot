"""Единая точка размерностей obs (без импортов — чтобы не создавать циклов).

Почему отдельный модуль, а не константа в config.py: цепочка импортов
    config -> action_space -> features
циклична, поэтому features.py физически не может прочитать config.N_FEATURES. Раньше это
маскировалось `try/except ImportError: N_FEATURES = 991` — и после добавления новых
per-move блоков (AUDIT 33) раскладка стала 1119, а «видимое» значение осталось 991.
Именно так и родился бы тихий рассинхрон: obs 1119 признаков, чекпоинты/vecnorm — 991.

Правило: размерность объявляется здесь, а features.obs_layout() при импорте СВЕРЯЕТ
фактическую сумму блоков с этим числом и падает при расхождении. Дополнительно
test_features_verify проверяет совпадение с config.N_FEATURES.
"""

N_FEATURES = 1119
